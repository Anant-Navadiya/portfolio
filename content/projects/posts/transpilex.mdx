import { ProjectCallout, ProjectDecision, ProjectGallery, ProjectMetric, ProjectMetrics } from "@/components/projects/ProjectContent"


## Context

When I was working in the company Coderthemes Design LLP, my responsibility was to convert HTML pages in to various frameworks and I was working in many frameworks, around 20.

## The Problem

Now as I was working with so many frameworks if designing team creates one html design and I would have to create or convert that design into these frameworks, all static pages. And the steps to do this were the same but it would require hours to days depending on the framework and in all the project I was following the same steps.